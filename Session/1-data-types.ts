{
  // special data types- union, any, never, void.

  // Void Type: The void type is used as the return type of functions that do not return a value.
  function logMessage(message: string): void {
    console.log(message);
  }

  // ----- Enum data type (string | numeric | heterogeneous)

  //
}
